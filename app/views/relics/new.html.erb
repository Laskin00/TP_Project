<% provide(:title, 'Add Relic') %>

<h1 class = "white">Add Relic</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@relic) do |w| %>
      <div id = "button">
        <%= w.label :name %>
        <%= w.text_field :name, class: 'form-control' %>
      </div>
      <div id = "button">
        <%= w.label :image_url, "Image Url" %>
        <%= w.text_field :image_url, class: 'form-control' %>
      </div>
      <div id = "button">
        <%= w.label :whereToGet , "Where To Get"%>
        <%= w.text_area :whereToGet, class: 'form-control' %>
      </div>
      <div id = "button">
        <%= w.label :dropChance, "Drop Chance" %>
        <%= w.text_field :dropChance, class: 'form-control' %>
      </div>

      <div id = "button">
        <%= w.label :relic_type, "Relic Type" %>
        <%= w.text_field :relic_type, class: 'form-control' %>
      </div>

      <div id = "button">
        <%= w.submit "Add Relic", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div>
</div>

<% provide(:title, 'Add Warframe') %>
<h1 class = "white">Add Warframe</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@warframe) do |f| %>

      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>

      <%= f.label :image_url, "Image Url" %>
      <%= f.text_field :image_url, class: 'form-control' %>

      <%= f.label :whereToGet , "Where To Get"%>
      <%= f.text_area :whereToGet, class: 'form-control' %>

      <%= f.label :blueprintDropChance, "Blueprint Drop Chance" %>
      <%= f.text_field :blueprintDropChance, class: 'form-control' %>

      <%= f.submit "Add Mod", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
